---

- name: reload vault-agent
  ansible.builtin.systemd:
    name: vault-agent
    state: reloaded
    daemon_reload: true

- name: restart vault-agent
  ansible.builtin.systemd:
    name: vault-agent
    state: restarted
    daemon_reload: true
